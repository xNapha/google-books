import SearchBar from "../../components/SearchBar/SearchBar";

const HomePage = () => {
    return (
        <div>
            <h1>Google Books</h1>
            <SearchBar />
        </div>
    );
};

export default HomePage;
